# Secrets
.env
.strava_tokens.json
.gcal_tokens.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environment
venv
venv/
.venv/

# IDE
.vscode/
.idea/

# Tooling
.ruff_cache/
.pytest_cache/
htmlcov/

# Project
TODOS.md

# state/ runtime data — SQLite DB + WAL/SHM sidecars. Lives on a Railway
# volume in prod (mounted at /app/data) and stays gitignored locally. The
# legacy state/*.{md,yaml,jsonl,json} files are kept committed for now as
# migration seeds and will be pruned once the new backend is stable.
state/coach.db
state/coach.db-wal
state/coach.db-shm
state/.gcal_sync_state.json

# OS
.DS_Store
Thumbs.db
